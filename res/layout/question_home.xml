<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="fill_parent"
  android:layout_height="fill_parent"
  >
    <ImageButton
        android:id="@+id/addQuestionIcon"
        android:layout_width="50dip"
        android:layout_height="50dip"
        android:layout_toLeftOf="@+id/questionTagSelector"
        android:layout_toStartOf="@+id/questionTagSelector"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#00000000"
        android:src="@drawable/add_question_icon" />
    
    <Spinner
        android:id="@+id/questionTagSelector"
        android:layout_width="100dip"
        android:layout_height="50dip"
		android:layout_centerHorizontal="true"
		android:layout_alignParentTop="true"
        />

    <ImageButton
        android:id="@+id/settings"
        android:layout_width="50dip"
        android:layout_height="50dip"
        android:layout_toRightOf="@+id/questionTagSelector"
        android:layout_toEndOf="@+id/questionTagSelector"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="#00000000"
        android:src="@drawable/default_round_head" />

        <Button 
          	android:id="@+id/testButton"
          	android:layout_width="match_parent"
          	android:layout_height="wrap_content"
          	android:text="显示问题"
          	android:layout_below="@+id/questionTagSelector"
            />
    
        <ListView
	        android:id="@+id/listView"
	        android:layout_width="match_parent"
	        android:layout_height="match_parent"
	        android:layout_below="@+id/testButton">
	        <!-- 此处插入question item -->

   		</ListView>

</RelativeLayout>